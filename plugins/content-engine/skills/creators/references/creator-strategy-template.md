---
client: <slug>
type: creator-strategy
date: <YYYY-MM-DD>
status: raw
title: "<Client> — creator copy strategy"
run_id: <YYYY-MM-DD-<client>-creators-NN>
command: </content-engine:creators | null>
skills: [<from run-log resolver>]
plugins: [<from run-log resolver>]
connectors: [<from run-log resolver>]
sources: [<the creator cards this was compiled from>]
creators: [<creator-slugs selected>]
---

# <Client> — creator copy strategy

Up: [Creator roster](index.md)

> 🎯 The copy-this strategy for <client>, compiled from <N> selected creators/posts/patterns on <YYYY-MM-DD>. Every example traces to a card.

## Post strategy

<One block per chosen pattern. For each: the pattern (named plainly), who
we're copying it from (link to their card), why it fits — mapped to the
client's actual pillar and voice rules from context.md — and the adaptation
needed. Nothing generic: "contrast-aphorism hooks from [creator] for the
[pillar] pillar", not "post more hooks.">

## Not copying

<The explicit exclusions and why: off-voice (breaks a tone rule or banned
phrase), off-ICP, breaks a Canon hard constraint, or unsustainable. This
section keeps the strategy honest when someone else executes it.>

## Posting schedule

<A concrete weekly grid: day → pillar → pattern → source creator. Reconciled
with the client's own cadence from context.md — state both numbers ("creator
posts daily; client cadence is 3/week; schedule uses 3 slots").>

| Day | Pillar | Pattern | Copied from |
|---|---|---|---|

## Example posts

<The selected winners, full text verbatim, each under `### <hook>` with the
post URL and one line naming the transferable move. These are the swipe
references the drafter reads.>

## Library of posts to copy

<Every selected post in one table — the working library for drafting
sessions.>

| Hook | Creator | Engagement | URL | What to steal |
|---|---|---|---|---|

## Next steps

<Pointers, not auto-runs: draft via copy-post with a library post as the
example; compile a creator into a style profile via style-library; spec a
format via scout. Name the exact posts/creators to start with.>
